These functions are located in graph.py.

categoryBarGraph(rowlist)

Argument passed rowlist is a list of mlRow objects.  The function then displays a graph showing the amount in each category.  Graph may need to be maximized to fully display categories.
